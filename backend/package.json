{
  "name": "trustflow-finance-backend",
  "version": "2.0.0",
  "description": "TrustFlow Finance — NBFC Loan Chatbot Backend API",
  "main": "src/server.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "seed": "node src/scripts/seedCustomers.js",
    "test": "jest --runInBand",
    "test:unit": "jest --testPathPattern=modules --runInBand",
    "test:integration": "jest --testPathPattern=tests --runInBand",
    "test:coverage": "jest --coverage --runInBand"
  },
  "dependencies": {
    "argon2": "^0.45.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.2.0",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.3.0",
    "morgan": "^1.10.0",
    "multer": "^2.1.1",
    "uuid": "^11.1.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "mongodb-memory-server": "^9.1.1",
    "nodemon": "^3.1.0",
    "supertest": "^6.3.4"
  }
}
